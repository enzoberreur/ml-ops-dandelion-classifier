# Screenshots

Place the screenshots used in the main `README.md` here. Suggested files:

- `mlflow-run.png` - capture of the latest MLflow run metrics (accuracy, loss curves).
- `minio-artifacts.png` - MinIO console showing the `dandelion-models` and `mlflow-artifacts` buckets.
- `grafana-airflow.png` - Grafana Airflow dashboard with scheduler metrics.
- `grafana-api.png` - Grafana API dashboard with `/predict` latency and traffic.

Update the image references in `README.md` once the files are available.
